version: '3'

services:

#  web:
#    image: nginx
#    volumes:
#      - ./templates:/etc/nginx/templates
#    ports:
#      - "80:80"
#    environment:
#      - NGINX_HOST=foobar.com
#      - NGINX_PORT=80
  db:
    image: mysql:latest
    container_name: db
    environment:
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - dbdata:/var/lib/mysql
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: pma
    links:
      - db
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
    restart: always
    ports:
      - 8181:80
  app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - /home/java15/uploads:/uploads
    depends_on:
      - db

volumes:
  dbdata:
  